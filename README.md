# homebrew-tap

Homebrew tap for [Markdown Better View](https://markdownbetterview.com) tools.

```sh
brew install xxBedy/tap/mbv
```

`mbv` opens a Markdown file in Markdown Better View from the terminal
(`mbv README.md`); the app live-reloads on every save.
